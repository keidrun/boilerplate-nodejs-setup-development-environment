# example-nodejs-setup-dev-env-for-multi-os

Example to set up nodejs development environment for multi os (Linux/Mac/Windows).

## Set up

* Clone this repo
* `yarn db:migrate`
* `chmod +x run`
* `yarn setup`
* Open `http://web.dev/` and `http://web.dev/db/`

## Destroy

* `yarn destroy`
